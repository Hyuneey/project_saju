{
  "name": "project-saju",
  "private": true,
  "version": "0.1.1",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "check:encoding": "node scripts/check-encoding.mjs",
    "dev": "corepack pnpm --filter web dev",
    "test": "vitest run",
    "typecheck": "corepack pnpm -r typecheck",
    "verify": "corepack pnpm check:encoding && corepack pnpm test && corepack pnpm typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
